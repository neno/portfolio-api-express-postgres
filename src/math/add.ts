const myArray = [
  1,2,
3
]

function doSomeStuff(
arr) {
  for (let nr of
    arr) {
      console.log("number: " + nr);
      
    }
}

export function add(a: number, b: number): number {
  return a + b;
}
